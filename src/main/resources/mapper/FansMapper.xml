<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twitter.twitterplusp.mapper.FansMapper">

    <select id="getPpRelation" resultType="java.lang.Integer">
        SELECT 1 FROM tb_follow WHERE user_id = #{loginUid} AND follow_user_id = #{otherUid}
        union ALL
        SELECT 2 FROM tb_follow WHERE user_id = #{otherUid} AND follow_user_id = #{loginUid}
    </select>
</mapper>